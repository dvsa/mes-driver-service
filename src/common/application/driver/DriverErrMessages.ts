/* eslint-disable */
export enum DriverErrorMessages {
    BAD_REQUEST = 'No drivingLicenceNumber detected in path param',
    NOT_FOUND = 'Driver not found with info supplied',
    INTERNAL_SERVER_ERROR = 'Internal server error',
    INVALID = 'Parameters are invalid'
}
